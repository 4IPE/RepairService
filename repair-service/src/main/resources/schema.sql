CREATE TABLE IF NOT EXISTS requests (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL UNIQUE,
    problem VARCHAR,
    model VARCHAR,
    oldTechnique VARCHAR ,
    nameComplainant VARCHAR,
    numberComplainant VARCHAR,
    CONSTRAINT pk_requests PRIMARY KEY (id)
    );